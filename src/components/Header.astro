---
import { SITE } from "../consts";
import HeaderAvatar from "./HeaderAvatar.astro";
import HeaderButtonBack from "./HeaderButtonBack.astro";
import HeaderButtonBackToTop from "./HeaderButtonBackToTop.astro";

const { pageName } = Astro.props;
---

<header id="header" class="sticky top-0 z-[99997]">
    <div class="container mx-auto px-8 py-2.5">
        <div class="flex justify-between items-center">
            <h1 class="flex items-center">
                <a href="/">{SITE.TITLE}</a>
                <span id="loadingIcon" class="ml-1.5 p-1 rounded-md">
                    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="size-3 animate-spin">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M16.023 9.348h4.992v-.001M2.985 19.644v-4.992m0 0h4.992m-4.993 0 3.181 3.183a8.25 8.25 0 0 0 13.803-3.7M4.031 9.865a8.25 8.25 0 0 1 13.803-3.7l3.181 3.182m0-4.991v4.99" />
                    </svg>
                </span>
            </h1>
            <div class="flex items-center gap-2">
                <HeaderButtonBackToTop />
                {pageName !== 'index' && <HeaderButtonBack />}
                {pageName === 'index' && <HeaderAvatar avatar={SITE.AVATAR} />}
            </div>
        </div>
    </div>
</header>